require(['jquery'],function($){'use strict';$(document).on('swatch.initialized',function(e){$('.swatch-attribute .swatch-option:not(.selected):nth-child(1)').trigger('click');$('.swatch-label-count').on('click',function(){window.location=$(this).closest('.product-item').find('.product-item-link').attr('href');});});});